
void create_graph(int int_data, char str_label[9000]) {
  
    printf("\n%s(%d):", str_label, int_data);
    printf("\n");
    int i = 0;
    while (i < int_data) {
        if (int_data > 71) {
            printf("\n#");
        } else {
            printf("#");
        }
        i++;
    }
}
